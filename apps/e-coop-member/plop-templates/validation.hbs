import z from 'zod'

import { entityIdSchema, descriptionTransformerSanitizer } from '@e-coop/utils'

export const {{pascalCase name}}Schema = z.object({
    id: entityIdSchema.optional(),
    name: z.string().min(1, '{{pascalCase name}} name is required'),
    description: z.string()
        .min(10, 'Min 10 character description')
        .optional()
        .transform(descriptionTransformerSanitizer),
})

export type T{{pascalCase name}}Schema = z.infer<typeof {{pascalCase name}}Schema>
