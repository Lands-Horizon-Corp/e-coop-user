import { communicationsModels } from './communications-models'

describe('communicationsModels', () => {
    it('should work', () => {
        expect(communicationsModels()).toEqual('communications-models')
    })
})
