import { transactionsModels } from './transactions-models'

describe('transactionsModels', () => {
    it('should work', () => {
        expect(transactionsModels()).toEqual('transactions-models')
    })
})

export {};
