import { socialMediaModels } from './social-media-models'

describe('socialMediaModels', () => {
    it('should work', () => {
        expect(socialMediaModels()).toEqual('social-media-models')
    })
})
