<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Income Statement - A5</title>
<style>
    @page {
        margin: 0;
    }

    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    html, body {
        width: 5.83in;
        height: 8.27in;
        margin: 0;
        padding: 0;
    }

    body {
        background-color: #f9fafb;
        padding: 0.3in;
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .is-container {
        /* Fixed font sizes for A5 print */
        --fs-xs: 7px;
        --fs-sm: 8px;
        --fs-base: 9px;
        --fs-lg: 10px;
        --fs-xl: 11px;
        --fs-2xl: 14px;
        --primary: #008000;

        width: 5.83in;
        height: 8.27in;
        padding: 0.3in;
        background: white;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Inter', 'Helvetica Neue', Arial, sans-serif;
        color: #111827;
        font-size: var(--fs-base);
        line-height: 1.4;
        display: flex;
        flex-direction: column;
        box-sizing: border-box;
    }

    /* Header */
    .is-header {
        text-align: center;
        margin-bottom: 0.75rem;
        padding-bottom: 0.5rem;
    }

    .is-header-title {
        font-size: var(--fs-xl);
        font-weight: 700;
        color: #111827;
        margin-bottom: 0.15rem;
        letter-spacing: -0.025em;
        line-height: 1.2;
    }

    .is-header-address {
        font-size: var(--fs-sm);
        color: #6b7280;
        margin: 0.15rem 0;
        line-height: 1.3;
    }

    .is-header-tin {
        font-size: var(--fs-xs);
        color: #9ca3af;
        margin-top: 0.1rem;
    }

    .is-report-title {
        font-size: var(--fs-lg);
        font-weight: 700;
        text-transform: uppercase;
        margin-top: 0.3rem;
        letter-spacing: 0.05em;
    }

    /* Period Info */
    .is-period-info {
        text-align: center;
        font-size: var(--fs-sm);
        color: #6b7280;
        margin-bottom: 0.6rem;
        padding: 0.3rem;
        background: #f9fafb;
        border-radius: 0.25rem;
        font-weight: 600;
    }

    /* Section */
    .is-section {
        margin-bottom: 0.6rem;
    }

    .is-section-heading {
        font-size: var(--fs-base);
        font-weight: 700;
        color: #111827;
        margin: 0.5rem 0 0.3rem 0;
        text-transform: uppercase;
        background: #f3f4f6;
        padding: 0.3rem 0.5rem;
        border-left: 3px solid var(--primary);
    }

    /* Table */
    .is-table {
        width: 100%;
        border-collapse: collapse;
        margin-bottom: 0.3rem;
        font-size: var(--fs-sm);
    }

    .is-table th,
    .is-table td {
        padding: 0.3rem 0.5rem;
        text-align: left;
        border-bottom: 1px solid #f3f4f6;
    }

    .is-table th {
        font-weight: 700;
        background: linear-gradient(to bottom, #f9fafb, #f3f4f6);
        font-size: var(--fs-xs);
        color: #374151;
        text-transform: uppercase;
        letter-spacing: 0.025em;
        border-bottom: 2px solid #e5e7eb;
    }

    .is-table td {
        color: #111827;
    }

    .is-table .is-amount {
        text-align: right;
        width: 30%;
        font-family: 'SF Mono', 'Monaco', 'Courier New', monospace;
        font-variant-numeric: tabular-nums;
    }

    .is-subtotal-row {
        background: #f9fafb;
        font-weight: 700;
        border-top: 1px solid #d1d5db;
        border-bottom: 1px solid #d1d5db;
    }

    .is-subtotal-row td {
        padding: 0.4rem 0.5rem;
        color: #374151;
    }

    .is-net-income-row {
        font-weight: 800;
        font-size: var(--fs-lg);
    }

    .is-net-income-row td {
        padding: 0.5rem;
    }

    /* Signatures */
    .is-signature-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 0.6rem;
        margin-top: 1rem;
        padding: 0.5rem;
        border-radius: 0.25rem;
    }

    .is-signature-block {
        text-align: center;
    }

    .is-signature-label {
        font-size: var(--fs-xs);
        color: #6b7280;
        margin-bottom: 0.3rem;
        text-transform: uppercase;
        letter-spacing: 0.025em;
        font-weight: 600;
    }

    .is-signature-line {
        border-bottom: 0.5px solid #424e6167;
        padding: 0.3rem 0;
        min-height: 0.8rem;
        font-weight: 600;
        font-size: var(--fs-sm);
        margin-top: 0.3rem;
    }

    /* Footer */
    .is-footer {
        display: grid;
        grid-template-columns: 1fr 1fr 1fr;
        gap: 0.3rem;
        font-size: var(--fs-xs);
        color: #6b7280;
        border-top: 1px solid #e5e7eb;
        padding-top: 0.5rem;
        margin-top: auto;
    }

    .is-footer-left {
        text-align: left;
    }

    .is-footer-center {
        text-align: center;
    }

    .is-footer-right {
        text-align: right;
    }

    /* Print optimization */
    @media print {
        html, body {
            width: 5.83in;
            height: 8.27in;
            margin: 0;
            padding: 0;
        }

        body {
            background-color: white;
            padding: 0;
        }

        .is-container {
            padding: 0.3in;
            box-shadow: none;
        }

        * {
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
        }
    }
</style>
</head>
<body>
    <div class="is-container">
        <header class="is-header">
            <h1 class="is-header-title">{{header_title}}</h1>
            <p class="is-header-address">{{header_address}}</p>
            <p class="is-header-tin">TIN: {{tax_number}}</p>
            <h2 class="is-report-title">{{report_title}}</h2>
        </header>

        <div class="is-period-info">
            For the Period: {{date_from}} to {{date_to}}
        </div>

        <div class="is-section">
            <div class="is-section-heading">REVENUE</div>
            <table class="is-table">
                <thead>
                    <tr>
                        <th>Account Title</th>
                        <th class="is-amount">Amount</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each revenue_items}}
                    <tr>
                        <td>{{account_title}}</td>
                        <td class="is-amount">{{amount}}</td>
                    </tr>
                    {{/each}}
                    <tr class="is-subtotal-row">
                        <td>Total Revenue</td>
                        <td class="is-amount">{{total_revenue}}</td>
                    </tr>
                </tbody>
            </table>
        </div>

        <div class="is-section">
            <div class="is-section-heading">COST OF SALES</div>
            <table class="is-table">
                <thead>
                    <tr>
                        <th>Account Title</th>
                        <th class="is-amount">Amount</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each cost_of_sales_items}}
                    <tr>
                        <td>{{account_title}}</td>
                        <td class="is-amount">{{amount}}</td>
                    </tr>
                    {{/each}}
                    <tr class="is-subtotal-row">
                        <td>Total Cost of Sales</td>
                        <td class="is-amount">{{total_cost_of_sales}}</td>
                    </tr>
                    <tr class="is-subtotal-row">
                        <td>Gross Profit</td>
                        <td class="is-amount">{{gross_profit}}</td>
                    </tr>
                </tbody>
            </table>
        </div>

        <div class="is-section">
            <div class="is-section-heading">OPERATING EXPENSES</div>
            <table class="is-table">
                <thead>
                    <tr>
                        <th>Account Title</th>
                        <th class="is-amount">Amount</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each operating_expense_items}}
                    <tr>
                        <td>{{account_title}}</td>
                        <td class="is-amount">{{amount}}</td>
                    </tr>
                    {{/each}}
                    <tr class="is-subtotal-row">
                        <td>Total Operating Expenses</td>
                        <td class="is-amount">{{total_operating_expenses}}</td>
                    </tr>
                </tbody>
            </table>
        </div>

        <table class="is-table">
            <tbody>
                <tr class="is-net-income-row">
                    <td>NET INCOME</td>
                    <td class="is-amount">{{net_income}}</td>
                </tr>
            </tbody>
        </table>

        <div class="is-signature-grid">
            <div class="is-signature-block">
                <p class="is-signature-label">Prepared by:</p>
                <div class="is-signature-line">{{prepared_by}}</div>
            </div>
            <div class="is-signature-block">
                <p class="is-signature-label">Checked by:</p>
                <div class="is-signature-line">{{checked_by}}</div>
            </div>
            <div class="is-signature-block">
                <p class="is-signature-label">Approved by:</p>
                <div class="is-signature-line">{{approved_by}}</div>
            </div>
        </div>

        <div class="is-footer">
            <div class="is-footer-left">User: {{user_id}}</div>
            <div class="is-footer-center">{{report_date}}</div>
            <div class="is-footer-right">{{time}}</div>
        </div>
    </div>
</body>
</html>
