<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Income Statement</title>
<style>
    @page {
        margin: 0;
    }

    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    html, body {
        width: 8.5in;
        height: 11in;
        margin: 0;
        padding: 0;
    }

    body {
        background-color: #f9fafb;
        padding: 0.5in;
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .is-container {
        /* Fixed font sizes for 8.5x11 print */
        --fs-xs: 9px;
        --fs-sm: 10px;
        --fs-base: 11px;
        --fs-lg: 14px;
        --fs-xl: 16px;
        --fs-2xl: 20px;
        --primary: #008000;

        width: 8.5in;
        height: 11in;
        padding: 0.6in;
        background: white;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Inter', 'Helvetica Neue', Arial, sans-serif;
        color: #111827;
        font-size: var(--fs-base);
        line-height: 1.5;
        display: flex;
        flex-direction: column;
        box-sizing: border-box;
    }

    /* Header */
    .is-header {
        text-align: center;
        margin-bottom: 1.25rem;
        padding-bottom: 0.75rem;
    }

    .is-header-title {
        font-size: var(--fs-xl);
        font-weight: 700;
        color: #111827;
        margin-bottom: 0.25rem;
        letter-spacing: -0.025em;
        line-height: 1.2;
    }

    .is-header-address {
        font-size: var(--fs-sm);
        color: #6b7280;
        margin: 0.25rem 0;
        line-height: 1.4;
    }

    .is-header-tin {
        font-size: var(--fs-xs);
        color: #9ca3af;
        margin-top: 0.125rem;
    }

    .is-report-title {
        font-size: var(--fs-lg);
        font-weight: 700;
        text-transform: uppercase;
        margin-top: 0.5rem;
        letter-spacing: 0.05em;
    }

    /* Period Info */
    .is-period-info {
        text-align: center;
        font-size: var(--fs-sm);
        color: #6b7280;
        margin-bottom: 1rem;
        padding: 0.5rem;
        background: #f9fafb;
        border-radius: 0.375rem;
        font-weight: 600;
    }

    /* Entry Rows */
    .is-entry-row {
        display: grid;
        grid-template-columns: 2fr 1fr;
        gap: 0.5rem;
        padding: 0.35rem 0.75rem 0.35rem 1.5rem;
        font-size: var(--fs-sm);
        border-bottom: 1px solid #f3f4f6;
    }

    .is-entry-row.with-comparison {
        grid-template-columns: 2fr 1fr 1fr 80px 70px;
    }

    .is-entry-description {
        color: #1f2937;
    }

    .is-entry-amount {
        text-align: right;
        font-family: 'SF Mono', 'Monaco', 'Courier New', monospace;
        font-variant-numeric: tabular-nums;
        color: #1f2937;
    }

    .is-entry-change {
        text-align: center;
        font-size: var(--fs-xs);
        font-weight: 700;
    }

    .is-entry-change.increase {
        color: #059669;
    }

    .is-entry-change.decrease {
        color: #dc2626;
    }

    .is-entry-percentage {
        text-align: right;
        font-size: var(--fs-xs);
        font-weight: 600;
        color: #6b7280;
    }

    /* Subtotal Row */
    .is-subtotal-row {
        display: grid;
        grid-template-columns: 2fr 1fr;
        gap: 0.5rem;
        padding: 0.5rem 0.75rem;
        font-size: var(--fs-sm);
        font-weight: 700;
        background: #f9fafb;
        border-top: 1px solid #d1d5db;
        border-bottom: 1px solid #d1d5db;
        margin-top: 0.25rem;
    }

    .is-subtotal-row.with-comparison {
        grid-template-columns: 2fr 1fr 1fr 80px 70px;
    }

    .is-subtotal-label {
        color: #374151;
    }

    .is-subtotal-amount {
        text-align: right;
        font-family: 'SF Mono', 'Monaco', 'Courier New', monospace;
        font-variant-numeric: tabular-nums;
        color: #1f2937;
    }

    .is-subtotal-change {
        text-align: center;
        font-size: var(--fs-sm);
        font-weight: 700;
    }

    .is-subtotal-change.increase {
        color: #059669;
    }

    .is-subtotal-change.decrease {
        color: #dc2626;
    }

    .is-subtotal-percentage {
        text-align: right;
        font-size: var(--fs-sm);
        font-weight: 700;
        color: #6b7280;
    }

    /* Total Row */
    .is-total-row {
        display: grid;
        grid-template-columns: 2fr 1fr;
        gap: 0.5rem;
        padding: 0.625rem 0.75rem;
        font-size: var(--fs-base);
        font-weight: 700;
        background: #f3f4f6;
        border-top: 2px solid #9ca3af;
        border-bottom: 2px solid #9ca3af;
        margin: 0.75rem 0;
    }

    .is-total-row.with-comparison {
        grid-template-columns: 2fr 1fr 1fr 80px 70px;
    }

    .is-total-label {
        color: #111827;
        text-transform: uppercase;
    }

    .is-total-amount {
        text-align: right;
        font-family: 'SF Mono', 'Monaco', 'Courier New', monospace;
        font-variant-numeric: tabular-nums;
        color: #111827;
    }

    .is-total-change {
        text-align: center;
        font-size: var(--fs-sm);
        font-weight: 700;
    }

    .is-total-change.increase {
        color: #059669;
    }

    .is-total-change.decrease {
        color: #dc2626;
    }

    .is-total-percentage {
        text-align: right;
        font-size: var(--fs-sm);
        font-weight: 700;
        color: #6b7280;
    }

    /* Section */
    .is-section {
        margin-bottom: 1rem;
    }

    .is-section-heading {
        font-size: var(--fs-base);
        font-weight: 700;
        color: #111827;
        margin: 0.75rem 0 0.5rem 0;
        text-transform: uppercase;
        background: #f3f4f6;
        padding: 0.5rem 0.75rem;
        border-left: 3px solid var(--primary);
    }

    .is-account-group {
        margin-bottom: 0.75rem;
    }

    .is-account-title {
        font-size: var(--fs-sm);
        font-weight: 700;
        color: #374151;
        margin: 0.5rem 0 0.25rem 0;
        padding-left: 0.75rem;
    }

    /* Signatures */
    .is-signature-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 1rem;
        margin-top: 1.5rem;
        padding: 0.75rem;
        border-radius: 0.375rem;
    }

    .is-signature-block {
        text-align: center;
    }

    .is-signature-label {
        font-size: var(--fs-xs);
        color: #6b7280;
        margin-bottom: 0.5rem;
        text-transform: uppercase;
        letter-spacing: 0.025em;
        font-weight: 600;
    }

    .is-signature-line {
        border-bottom: 0.5px solid #424e6167;
        padding: 0.5rem 0;
        min-height: 1rem;
        font-weight: 600;
        font-size: var(--fs-sm);
        margin-top: 0.5rem;
    }

    .is-footer {
        display: grid;
        grid-template-columns: 1fr 1fr 1fr;
        gap: 0.25rem;
        font-size: var(--fs-xs);
        color: #6b7280;
        border-top: 1px solid #e5e7eb;
        padding-top: 0.3rem;
        margin-top: auto;
        width: 100%;
    }

    .is-footer-left {
        text-align: left;
    }

    .is-footer-center {
        text-align: center;
    }

    .is-footer-right {
        text-align: right;
    }

    /* Print optimization */
    @media print {
        html, body {
            width: 8.5in;
            height: 11in;
            margin: 0;
            padding: 0;
        }

        body {
            background-color: white;
            padding: 0;
        }

        .is-container {
            padding: 0.5in;
            box-shadow: none;
        }

        * {
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
        }
    }
</style>
</head>
<body>
    <div class="is-container">
        <header class="is-header">
            <h1 class="is-header-title">{{header_title}}</h1>
            <p class="is-header-address">{{header_address}}</p>
            <p class="is-header-tin">TIN: {{tax_number}}</p>
            <h2 class="is-report-title">{{report_title}}</h2>
        </header>

        <div class="is-period-info">
            For the Period: {{date_from}} to {{date_to}}
        </div>

        {{! ============ INCOME SECTION ============ }}
        <div class="is-section">
            <div class="is-section-heading">INCOME</div>

            {{! Column Headers - Conditional based on report type }}
            {{#if (equals report_type "COMPARATIVE_MONTHLY")}}
            <div class="is-entry-row with-comparison" style="font-weight: 700; background: #f9fafb; font-size: var(--fs-xs); color: #374151; text-transform: uppercase;">
                <div>Account</div>
                <div style="text-align: right;">Current</div>
                <div style="text-align: right;">Previous</div>
                <div style="text-align: center;">Inc/Dec</div>
                <div style="text-align: right;">%</div>
            </div>
            {{/if}}
            {{#if (equals report_type "COMPARATIVE_YEARLY")}}
            <div class="is-entry-row with-comparison" style="font-weight: 700; background: #f9fafb; font-size: var(--fs-xs); color: #374151; text-transform: uppercase;">
                <div>Account</div>
                <div style="text-align: right;">Current Year</div>
                <div style="text-align: right;">Previous Year</div>
                <div style="text-align: center;">Inc/Dec</div>
                <div style="text-align: right;">%</div>
            </div>
            {{/if}}
            {{#if (equals report_type "BUDGET_FORECASTING")}}
            <div class="is-entry-row with-comparison" style="font-weight: 700; background: #f9fafb; font-size: var(--fs-xs); color: #374151; text-transform: uppercase;">
                <div>Account</div>
                <div style="text-align: right;">Actual</div>
                <div style="text-align: right;">Budget</div>
                <div style="text-align: center;">Variance</div>
                <div style="text-align: right;">%</div>
            </div>
            {{/if}}

            {{! Income Entries - Grouped by account_title }}
            {{#each income_entries}}
                {{#if account_title}}
                <div class="is-account-title">{{account_title}}</div>
                {{/if}}
                {{#each account_entries}}
                    {{#if (equals ../../report_type "STANDARD")}}
                    <div class="is-entry-row">
                        <div class="is-entry-description">{{description}}</div>
                        <div class="is-entry-amount">{{current_amount}}</div>
                    </div>
                    {{/if}}
                    {{#if (equals ../../report_type "COMPARATIVE_MONTHLY")}}
                    <div class="is-entry-row with-comparison">
                        <div class="is-entry-description">{{description}}</div>
                        <div class="is-entry-amount">{{current_amount}}</div>
                        <div class="is-entry-amount">{{previous_amount}}</div>
                        <div class="is-entry-change {{#if inc_or_dec_amount}}{{#if (equals inc_or_dec_amount "INC")}}increase{{else}}decrease{{/if}}{{/if}}">{{inc_or_dec_amount}}</div>
                        <div class="is-entry-percentage">{{percentage}}%</div>
                    </div>
                    {{/if}}
                    {{#if (equals ../../report_type "COMPARATIVE_YEARLY")}}
                    <div class="is-entry-row with-comparison">
                        <div class="is-entry-description">{{description}}</div>
                        <div class="is-entry-amount">{{current_amount}}</div>
                        <div class="is-entry-amount">{{previous_year_amount}}</div>
                        <div class="is-entry-change {{#if inc_or_dec_amount}}{{#if (equals inc_or_dec_amount "INC")}}increase{{else}}decrease{{/if}}{{/if}}">{{inc_or_dec_amount}}</div>
                        <div class="is-entry-percentage">{{percentage}}%</div>
                    </div>
                    {{/if}}
                    {{#if (equals ../../report_type "CLOSED_BOOK")}}
                    <div class="is-entry-row">
                        <div class="is-entry-description">{{description}}</div>
                        <div class="is-entry-amount">{{current_amount}}</div>
                    </div>
                    {{/if}}
                    {{#if (equals ../../report_type "BUDGET_FORECASTING")}}
                    <div class="is-entry-row with-comparison">
                        <div class="is-entry-description">{{description}}</div>
                        <div class="is-entry-amount">{{actual_amount}}</div>
                        <div class="is-entry-amount">{{budget_amount}}</div>
                        <div class="is-entry-change {{#if variance_amount}}{{#if (equals variance_amount "Favorable")}}increase{{else}}decrease{{/if}}{{/if}}">{{variance_amount}}</div>
                        <div class="is-entry-percentage">{{percentage}}%</div>
                    </div>
                    {{/if}}
                {{/each}}
            {{/each}}

            {{! Total Income Row }}
            {{#if (equals report_type "STANDARD")}}
            <div class="is-subtotal-row">
                <div class="is-subtotal-label">TOTAL INCOME</div>
                <div class="is-subtotal-amount">{{total_income_current}}</div>
            </div>
            {{/if}}
            {{#if (equals report_type "COMPARATIVE_MONTHLY")}}
            <div class="is-subtotal-row with-comparison">
                <div class="is-subtotal-label">TOTAL INCOME</div>
                <div class="is-subtotal-amount">{{total_income_current}}</div>
                <div class="is-subtotal-amount">{{total_income_previous}}</div>
                <div class="is-subtotal-change {{#if total_income_inc_or_dec}}{{#if (equals total_income_inc_or_dec "INC")}}increase{{else}}decrease{{/if}}{{/if}}">{{total_income_inc_or_dec}}</div>
                <div class="is-subtotal-percentage">{{total_income_percentage}}%</div>
            </div>
            {{/if}}
            {{#if (equals report_type "COMPARATIVE_YEARLY")}}
            <div class="is-subtotal-row with-comparison">
                <div class="is-subtotal-label">TOTAL INCOME</div>
                <div class="is-subtotal-amount">{{total_income_current}}</div>
                <div class="is-subtotal-amount">{{total_income_previous_year}}</div>
                <div class="is-subtotal-change {{#if total_income_inc_or_dec}}{{#if (equals total_income_inc_or_dec "INC")}}increase{{else}}decrease{{/if}}{{/if}}">{{total_income_inc_or_dec}}</div>
                <div class="is-subtotal-percentage">{{total_income_percentage}}%</div>
            </div>
            {{/if}}
            {{#if (equals report_type "CLOSED_BOOK")}}
            <div class="is-subtotal-row">
                <div class="is-subtotal-label">TOTAL INCOME</div>
                <div class="is-subtotal-amount">{{total_income_current}}</div>
            </div>
            {{/if}}
            {{#if (equals report_type "BUDGET_FORECASTING")}}
            <div class="is-subtotal-row with-comparison">
                <div class="is-subtotal-label">TOTAL INCOME</div>
                <div class="is-subtotal-amount">{{total_income_actual}}</div>
                <div class="is-subtotal-amount">{{total_income_budget}}</div>
                <div class="is-subtotal-change {{#if total_income_variance}}{{#if (equals total_income_variance "Favorable")}}increase{{else}}decrease{{/if}}{{/if}}">{{total_income_variance}}</div>
                <div class="is-subtotal-percentage">{{total_income_percentage}}%</div>
            </div>
            {{/if}}
        </div>

        {{! ============ EXPENSES SECTION ============ }}
        <div class="is-section">
            <div class="is-section-heading">EXPENSES</div>

            {{! Column Headers - Conditional based on report type }}
            {{#if (equals report_type "COMPARATIVE_MONTHLY")}}
            <div class="is-entry-row with-comparison" style="font-weight: 700; background: #f9fafb; font-size: var(--fs-xs); color: #374151; text-transform: uppercase;">
                <div>Account</div>
                <div style="text-align: right;">Current</div>
                <div style="text-align: right;">Previous</div>
                <div style="text-align: center;">Inc/Dec</div>
                <div style="text-align: right;">%</div>
            </div>
            {{/if}}
            {{#if (equals report_type "COMPARATIVE_YEARLY")}}
            <div class="is-entry-row with-comparison" style="font-weight: 700; background: #f9fafb; font-size: var(--fs-xs); color: #374151; text-transform: uppercase;">
                <div>Account</div>
                <div style="text-align: right;">Current Year</div>
                <div style="text-align: right;">Previous Year</div>
                <div style="text-align: center;">Inc/Dec</div>
                <div style="text-align: right;">%</div>
            </div>
            {{/if}}
            {{#if (equals report_type "BUDGET_FORECASTING")}}
            <div class="is-entry-row with-comparison" style="font-weight: 700; background: #f9fafb; font-size: var(--fs-xs); color: #374151; text-transform: uppercase;">
                <div>Account</div>
                <div style="text-align: right;">Actual</div>
                <div style="text-align: right;">Budget</div>
                <div style="text-align: center;">Variance</div>
                <div style="text-align: right;">%</div>
            </div>
            {{/if}}

            {{! Expense Entries - Grouped by account_title }}
            {{#each expense_entries}}
                {{#if account_title}}
                <div class="is-account-title">{{account_title}}</div>
                {{/if}}
                {{#each account_entries}}
                    {{#if (equals ../../report_type "STANDARD")}}
                    <div class="is-entry-row">
                        <div class="is-entry-description">{{description}}</div>
                        <div class="is-entry-amount">{{current_amount}}</div>
                    </div>
                    {{/if}}
                    {{#if (equals ../../report_type "COMPARATIVE_MONTHLY")}}
                    <div class="is-entry-row with-comparison">
                        <div class="is-entry-description">{{description}}</div>
                        <div class="is-entry-amount">{{current_amount}}</div>
                        <div class="is-entry-amount">{{previous_amount}}</div>
                        <div class="is-entry-change {{#if inc_or_dec_amount}}{{#if (equals inc_or_dec_amount "INC")}}increase{{else}}decrease{{/if}}{{/if}}">{{inc_or_dec_amount}}</div>
                        <div class="is-entry-percentage">{{percentage}}%</div>
                    </div>
                    {{/if}}
                    {{#if (equals ../../report_type "COMPARATIVE_YEARLY")}}
                    <div class="is-entry-row with-comparison">
                        <div class="is-entry-description">{{description}}</div>
                        <div class="is-entry-amount">{{current_amount}}</div>
                        <div class="is-entry-amount">{{previous_year_amount}}</div>
                        <div class="is-entry-change {{#if inc_or_dec_amount}}{{#if (equals inc_or_dec_amount "INC")}}increase{{else}}decrease{{/if}}{{/if}}">{{inc_or_dec_amount}}</div>
                        <div class="is-entry-percentage">{{percentage}}%</div>
                    </div>
                    {{/if}}
                    {{#if (equals ../../report_type "CLOSED_BOOK")}}
                    <div class="is-entry-row">
                        <div class="is-entry-description">{{description}}</div>
                        <div class="is-entry-amount">{{current_amount}}</div>
                    </div>
                    {{/if}}
                    {{#if (equals ../../report_type "BUDGET_FORECASTING")}}
                    <div class="is-entry-row with-comparison">
                        <div class="is-entry-description">{{description}}</div>
                        <div class="is-entry-amount">{{actual_amount}}</div>
                        <div class="is-entry-amount">{{budget_amount}}</div>
                        <div class="is-entry-change {{#if variance_amount}}{{#if (equals variance_amount "Unfavorable")}}decrease{{else}}increase{{/if}}{{/if}}">{{variance_amount}}</div>
                        <div class="is-entry-percentage">{{percentage}}%</div>
                    </div>
                    {{/if}}
                {{/each}}
            {{/each}}

            {{! Total Expenses Row }}
            {{#if (equals report_type "STANDARD")}}
            <div class="is-subtotal-row">
                <div class="is-subtotal-label">TOTAL EXPENSES</div>
                <div class="is-subtotal-amount">{{total_expense_current}}</div>
            </div>
            {{/if}}
            {{#if (equals report_type "COMPARATIVE_MONTHLY")}}
            <div class="is-subtotal-row with-comparison">
                <div class="is-subtotal-label">TOTAL EXPENSES</div>
                <div class="is-subtotal-amount">{{total_expense_current}}</div>
                <div class="is-subtotal-amount">{{total_expense_previous}}</div>
                <div class="is-subtotal-change {{#if total_expense_inc_or_dec}}{{#if (equals total_expense_inc_or_dec "INC")}}increase{{else}}decrease{{/if}}{{/if}}">{{total_expense_inc_or_dec}}</div>
                <div class="is-subtotal-percentage">{{total_expense_percentage}}%</div>
            </div>
            {{/if}}
            {{#if (equals report_type "COMPARATIVE_YEARLY")}}
            <div class="is-subtotal-row with-comparison">
                <div class="is-subtotal-label">TOTAL EXPENSES</div>
                <div class="is-subtotal-amount">{{total_expense_current}}</div>
                <div class="is-subtotal-amount">{{total_expense_previous_year}}</div>
                <div class="is-subtotal-change {{#if total_expense_inc_or_dec}}{{#if (equals total_expense_inc_or_dec "INC")}}increase{{else}}decrease{{/if}}{{/if}}">{{total_expense_inc_or_dec}}</div>
                <div class="is-subtotal-percentage">{{total_expense_percentage}}%</div>
            </div>
            {{/if}}
            {{#if (equals report_type "CLOSED_BOOK")}}
            <div class="is-subtotal-row">
                <div class="is-subtotal-label">TOTAL EXPENSES</div>
                <div class="is-subtotal-amount">{{total_expense_current}}</div>
            </div>
            {{/if}}
            {{#if (equals report_type "BUDGET_FORECASTING")}}
            <div class="is-subtotal-row with-comparison">
                <div class="is-subtotal-label">TOTAL EXPENSES</div>
                <div class="is-subtotal-amount">{{total_expense_actual}}</div>
                <div class="is-subtotal-amount">{{total_expense_budget}}</div>
                <div class="is-subtotal-change {{#if total_expense_variance}}{{#if (equals total_expense_variance "Unfavorable")}}decrease{{else}}increase{{/if}}{{/if}}">{{total_expense_variance}}</div>
                <div class="is-subtotal-percentage">{{total_expense_percentage}}%</div>
            </div>
            {{/if}}
        </div>

        {{! ============ NET SURPLUS ============ }}
        {{#if (equals report_type "STANDARD")}}
        <div class="is-total-row">
            <div class="is-total-label">NET SURPLUS</div>
            <div class="is-total-amount">{{net_surplus_current}}</div>
        </div>
        {{/if}}
        {{#if (equals report_type "COMPARATIVE_MONTHLY")}}
        <div class="is-total-row with-comparison">
            <div class="is-total-label">NET SURPLUS</div>
            <div class="is-total-amount">{{net_surplus_current}}</div>
            <div class="is-total-amount">{{net_surplus_previous}}</div>
            <div class="is-total-change {{#if net_surplus_inc_or_dec}}{{#if (equals net_surplus_inc_or_dec "INC")}}increase{{else}}decrease{{/if}}{{/if}}">{{net_surplus_inc_or_dec}}</div>
            <div class="is-total-percentage">{{net_surplus_percentage}}%</div>
        </div>
        {{/if}}
        {{#if (equals report_type "COMPARATIVE_YEARLY")}}
        <div class="is-total-row with-comparison">
            <div class="is-total-label">NET SURPLUS</div>
            <div class="is-total-amount">{{net_surplus_current}}</div>
            <div class="is-total-amount">{{net_surplus_previous_year}}</div>
            <div class="is-total-change {{#if net_surplus_inc_or_dec}}{{#if (equals net_surplus_inc_or_dec "INC")}}increase{{else}}decrease{{/if}}{{/if}}">{{net_surplus_inc_or_dec}}</div>
            <div class="is-total-percentage">{{net_surplus_percentage}}%</div>
        </div>
        {{/if}}
        {{#if (equals report_type "CLOSED_BOOK")}}
        <div class="is-total-row">
            <div class="is-total-label">NET SURPLUS</div>
            <div class="is-total-amount">{{net_surplus_current}}</div>
        </div>

        {{! Net Surplus Allocation Section (CLOSED_BOOK only) }}
        {{#if net_surplus_entries_for_allocation}}
        <div class="is-section" style="margin-top: 1rem;">
            <div class="is-section-heading">NET SURPLUS ALLOCATION</div>
            {{#each net_surplus_entries_for_allocation}}
            <div class="is-entry-row">
                <div class="is-entry-description">{{net_surplus_account_titles}}</div>
                <div class="is-entry-amount">{{amount}}</div>
            </div>
            {{/each}}
            <div class="is-subtotal-row">
                <div class="is-subtotal-label">TOTAL NET SURPLUS ALLOCATION</div>
                <div class="is-subtotal-amount">{{total_net_surplus_allocated}}</div>
            </div>
        </div>
        {{/if}}
        {{/if}}
        {{#if (equals report_type "BUDGET_FORECASTING")}}
        <div class="is-total-row with-comparison">
            <div class="is-total-label">NET SURPLUS</div>
            <div class="is-total-amount">{{net_surplus_actual}}</div>
            <div class="is-total-amount">{{net_surplus_budget}}</div>
            <div class="is-total-change {{#if net_surplus_variance}}{{#if (equals net_surplus_variance "Favorable")}}increase{{else}}decrease{{/if}}{{/if}}">{{net_surplus_variance}}</div>
            <div class="is-total-percentage">{{net_surplus_percentage}}%</div>
        </div>
        {{/if}}

        <div class="is-signature-grid">
            <div class="is-signature-block">
                <div class="is-signature-line">{{prepared_by}}</div>
                <p class="is-signature-label">Prepared by:</p>
            </div>
            <div class="is-signature-block">
                <div class="is-signature-line">{{checked_by}}</div>
                <p class="is-signature-label">Checked by:</p>
            </div>
            <div class="is-signature-block">
                <div class="is-signature-line">{{approved_by}}</div>
                <p class="is-signature-label">Approved by:</p>
            </div>
        </div>

        <div class="is-footer">
            <div class="">User: {{user_id}}</div>
            <div class="">{{report_date}} {{time}}</div>
            <div class="">Page {{page_number}}</div>
        </div>
    </div>
</body>
</html>