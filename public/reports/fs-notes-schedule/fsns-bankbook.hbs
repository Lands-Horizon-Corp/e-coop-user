<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FS Notes Schedule - Bankbook</title>
    <style>
        @page {
            margin: 0;
            size: 125mm 176mm;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html,
        body {
            width: 125mm;
            height: 176mm;
            margin: 0;
            padding: 0;
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
        }

        body {
            background-color: #f9fafb;
            padding: 8mm;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .fsns-container {
            /* Fixed font sizes for Bankbook print */
            --fs-xs: 6px;
            --fs-sm: 7px;
            --fs-base: 8px;
            --fs-lg: 9px;
            --fs-xl: 11px;
            --fs-2xl: 13px;

            width: 125mm;
            height: 176mm;
            padding: 8mm;
            background: white;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Inter', 'Helvetica Neue', Arial, sans-serif;
            color: #111827;
            font-size: var(--fs-base);
            line-height: 1.3;
            display: flex;
            flex-direction: column;
            box-sizing: border-box;
        }

        /* Header */
        .fsns-header {
            text-align: center;
            margin-bottom: 0.375rem;
            padding-bottom: 0.25rem;
        }

        .fsns-header h1 {
            font-size: var(--fs-xl);
            font-weight: 700;
            color: #111827;
            margin-bottom: 0.0625rem;
            letter-spacing: -0.025em;
            line-height: 1.2;
        }

        .fsns-header .fsns-address {
            font-size: var(--fs-sm);
            color: #6b7280;
            margin: 0.0625rem 0;
            line-height: 1.25;
        }

        .fsns-header .fsns-tin {
            font-size: var(--fs-xs);
            color: #9ca3af;
            margin-top: 0.0625rem;
        }

        .fsns-report-title {
            font-size: 10px;
            font-weight: 800;
            color: #111827;
            margin-top: 0.1875rem;
            letter-spacing: -0.025em;
            text-transform: uppercase;
        }

        .fsns-as-of-date {
            font-size: var(--fs-sm);
            color: #374151;
            margin-top: 0.1875rem;
            font-weight: 600;
        }

        /* Content Area */
        .fsns-content {
            flex: 1;
            overflow-y: auto;
            margin-bottom: 0.375rem;
        }

        /* Section Title */
        .fsns-section-title {
            font-size: var(--fs-base);
            font-weight: 700;
            color: #111827;
            margin: 0.375rem 0 0.1875rem 0;
            text-transform: uppercase;
            background: linear-gradient(to right, #f3f4f6, #e5e7eb);
            padding: 0.1875rem 0.25rem;
            border-left: 2px solid oklch(0.7227 0.192 149.5793);
        }

        /* Subsection Title */
        .fsns-subsection-title {
            font-size: var(--fs-sm);
            font-weight: 700;
            color: #374151;
            margin: 0.1875rem 0 0.0625rem 0;
            padding-left: 0.375rem;
        }

        /* Entry Row */
        .fsns-entry-row {
            display: grid;
            grid-template-columns: 2fr 1fr 1fr 35px;
            gap: 0.1875rem;
            padding: 0.125rem 0.25rem 0.125rem 0.5rem;
            font-size: var(--fs-sm);
            border-bottom: 1px solid #f3f4f6;
        }

        .fsns-entry-row.no-comparison {
            grid-template-columns: 2fr 1fr;
        }

        .fsns-entry-description {
            color: #1f2937;
        }

        .fsns-entry-amount {
            text-align: right;
            font-family: 'SF Mono', 'Monaco', 'Courier New', monospace;
            font-variant-numeric: tabular-nums;
            color: #1f2937;
        }

        .fsns-entry-change {
            text-align: center;
            font-size: var(--fs-xs);
            font-weight: 700;
        }

        .fsns-entry-change.increase {
            color: #059669;
        }

        .fsns-entry-change.decrease {
            color: #dc2626;
        }

        /* Subtotal Row */
        .fsns-subtotal-row {
            display: grid;
            grid-template-columns: 2fr 1fr 1fr 35px;
            gap: 0.1875rem;
            padding: 0.1875rem 0.25rem;
            font-size: var(--fs-sm);
            font-weight: 700;
            background: #f9fafb;
            border-top: 1px solid #d1d5db;
            border-bottom: 1px solid #d1d5db;
            margin-top: 0.0625rem;
        }

        .fsns-subtotal-label {
            color: #374151;
        }

        .fsns-subtotal-amount {
            text-align: right;
            font-family: 'SF Mono', 'Monaco', 'Courier New', monospace;
            font-variant-numeric: tabular-nums;
            color: #1f2937;
        }

        .fsns-subtotal-change {
            text-align: center;
            font-size: var(--fs-xs);
            font-weight: 700;
        }

        .fsns-subtotal-change.increase {
            color: #059669;
        }

        .fsns-subtotal-change.decrease {
            color: #dc2626;
        }

        /* Total Row */
        .fsns-total-row {
            display: grid;
            grid-template-columns: 2fr 1fr 1fr 35px;
            gap: 0.1875rem;
            padding: 0.25rem 0.25rem;
            font-size: var(--fs-base);
            font-weight: 700;
            background: #f3f4f6;
            border-top: 2px solid #9ca3af;
            border-bottom: 2px solid #9ca3af;
            margin: 0.25rem 0;
        }

        .fsns-total-label {
            color: #111827;
            text-transform: uppercase;
        }

        .fsns-total-amount {
            text-align: right;
            font-family: 'SF Mono', 'Monaco', 'Courier New', monospace;
            font-variant-numeric: tabular-nums;
            color: #111827;
        }

        .fsns-total-change {
            text-align: center;
            font-size: var(--fs-sm);
            font-weight: 700;
        }

        .fsns-total-change.increase {
            color: #059669;
        }

        .fsns-total-change.decrease {
            color: #dc2626;
        }

        /* Footer */
        .fsns-footer {
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 0.25rem;
            margin-top: auto;
            padding-top: 0.25rem;
            border-top: 1px solid #e5e7eb;
            font-size: var(--fs-xs);
            color: #6b7280;
        }

        .fsns-footer-item {
            display: inline-flex;
            align-items: center;
            gap: 0.0625rem;
        }

        .fsns-footer-label {
            font-weight: 700;
            color: #374151;
            text-transform: uppercase;
            letter-spacing: 0.025em;
        }

        .fsns-footer-value {
            color: #1f2937;
            font-family: monospace;
        }

        /* Print optimization */
        @media print {

            html,
            body {
                width: 125mm;
                height: 176mm;
                margin: 0;
                padding: 0;
            }

            body {
                background-color: white;
                padding: 0;
            }

            .fsns-container {
                padding: 8mm;
                box-shadow: none;
            }

            * {
                -webkit-print-color-adjust: exact;
                print-color-adjust: exact;
            }
        }
    </style>
</head>

<body>
    <div class="fsns-container">
        <header class="fsns-header">
            <h1>{{header_title}}</h1>
            <p class="fsns-address">{{header_address}}</p>
            <p class="fsns-tin">TIN: {{tax_number}}</p>
            <h2 class="fsns-report-title">{{report_title}}</h2>
            <p class="fsns-as-of-date">As of {{as_of_date}}</p>
        </header>

        <div class="fsns-content">
            <!-- Column Headers -->
            <div class="fsns-entry-row" style="font-weight: 700; background: #f9fafb; border-bottom: 2px solid #d1d5db; padding-top: 0.1875rem; padding-bottom: 0.1875rem;">
                <div class="fsns-entry-description" style="text-transform: uppercase; font-size: var(--fs-sm); color: #374151;">ASSETS</div>
                <div class="fsns-entry-amount" style="text-transform: uppercase; font-size: var(--fs-sm); color: #374151;">CURRENT</div>
                <div class="fsns-entry-amount" style="text-transform: uppercase; font-size: var(--fs-sm); color: #374151;">PREVIOUS</div>
                <div class="fsns-entry-change" style="text-transform: uppercase; font-size: var(--fs-sm); color: #374151;">INC/DEC</div>
            </div>

            <!-- Current Assets -->
            <div class="fsns-section-title">CURRENT ASSETS</div>

            {{#each current_assets.assets}}
            <div class="fsns-entry-row {{#unless previous_amount}}no-comparison{{/unless}}">
                <div class="fsns-entry-description">{{account_title}}</div>
                <div class="fsns-entry-amount">{{current_amount}}</div>
                {{#if previous_amount}}
                <div class="fsns-entry-amount">{{previous_amount}}</div>
                <div class="fsns-entry-change {{#if inc_or_dec}}{{#eq inc_or_dec 'INC'}}increase{{else}}decrease{{/eq}}{{/if}}">
                    {{inc_or_dec}}
                </div>
                {{/if}}
            </div>
            {{/each}}

            {{#if current_assets.loan_receivables}}
            <div class="fsns-subsection-title">Loan Receivables:</div>
            {{#each current_assets.loan_receivables}}
            <div class="fsns-entry-row {{#unless previous_amount}}no-comparison{{/unless}}">
                <div class="fsns-entry-description">{{account_title}}</div>
                <div class="fsns-entry-amount">{{current_amount}}</div>
                {{#if previous_amount}}
                <div class="fsns-entry-amount">{{previous_amount}}</div>
                <div class="fsns-entry-change {{#if inc_or_dec}}{{#eq inc_or_dec 'INC'}}increase{{else}}decrease{{/eq}}{{/if}}">
                    {{inc_or_dec}}
                </div>
                {{/if}}
            </div>
            {{/each}}

            <div class="fsns-subtotal-row">
                <div class="fsns-subtotal-label">Total Loan Recv.</div>
                <div class="fsns-subtotal-amount">{{current_assets.total_loans_receivables_current}}</div>
                <div class="fsns-subtotal-amount">{{current_assets.total_loans_receivables_previous}}</div>
                <div class="fsns-subtotal-change {{#if current_assets.total_loans_receivables_inc_or_dec}}{{#eq current_assets.total_loans_receivables_inc_or_dec 'INC'}}increase{{else}}decrease{{/eq}}{{/if}}">
                    {{current_assets.total_loans_receivables_inc_or_dec}}
                </div>
            </div>
            {{/if}}

            <div class="fsns-total-row">
                <div class="fsns-total-label">Total Current Assets</div>
                <div class="fsns-total-amount">{{total_current_assets_current}}</div>
                <div class="fsns-total-amount">{{total_current_assets_previous}}</div>
                <div class="fsns-total-change {{#if total_current_assets_inc_or_dec}}{{#eq total_current_assets_inc_or_dec 'INC'}}increase{{else}}decrease{{/eq}}{{/if}}">
                    {{total_current_assets_inc_or_dec}}
                </div>
            </div>

            <!-- Long Term Investment -->
            <div class="fsns-section-title">LONG-TERM INVEST.</div>

            <div class="fsns-entry-row">
                <div class="fsns-entry-description">Long-term Investment</div>
                <div class="fsns-entry-amount">{{long_term_investment_current}}</div>
                <div class="fsns-entry-amount">{{long_term_investment_previous}}</div>
                <div class="fsns-entry-change {{#if long_term_investment_inc_or_dec}}{{#eq long_term_investment_inc_or_dec 'INC'}}increase{{else}}decrease{{/eq}}{{/if}}">
                    {{long_term_investment_inc_or_dec}}
                </div>
            </div>

            <div class="fsns-total-row">
                <div class="fsns-total-label">Total LT Invest.</div>
                <div class="fsns-total-amount">{{total_long_term_investment_current}}</div>
                <div class="fsns-total-amount">{{total_long_term_investment_previous}}</div>
                <div class="fsns-total-change {{#if total_long_term_investment_inc_or_dec}}{{#eq total_long_term_investment_inc_or_dec 'INC'}}increase{{else}}decrease{{/eq}}{{/if}}">
                    {{total_long_term_investment_inc_or_dec}}
                </div>
            </div>

            <!-- Property and Equipment -->
            <div class="fsns-section-title">PROP. & EQUIP.</div>

            <div class="fsns-entry-row">
                <div class="fsns-entry-description">Property and Equipment</div>
                <div class="fsns-entry-amount">{{property_and_equipment_current}}</div>
                <div class="fsns-entry-amount">{{property_and_equipment_previous}}</div>
                <div class="fsns-entry-change {{#if property_and_equipment_inc_or_dec}}{{#eq property_and_equipment_inc_or_dec 'INC'}}increase{{else}}decrease{{/eq}}{{/if}}">
                    {{property_and_equipment_inc_or_dec}}
                </div>
            </div>

            <!-- Total Assets -->
            <div class="fsns-total-row">
                <div class="fsns-total-label">Total Assets</div>
                <div class="fsns-total-amount">{{total_assets_current}}</div>
                <div class="fsns-total-amount">{{total_assets_previous}}</div>
                <div class="fsns-total-change {{#if total_assets_inc_or_dec}}{{#eq total_assets_inc_or_dec 'INC'}}increase{{else}}decrease{{/eq}}{{/if}}">
                    {{total_assets_inc_or_dec}}
                </div>
            </div>
        </div>

        <footer class="fsns-footer">
            <div class="fsns-footer-item">
                <span class="fsns-footer-label">User ID:</span>
                <span class="fsns-footer-value">{{user_id}}</span>
            </div>
            <div class="fsns-footer-item">
                <span class="fsns-footer-label">Report Date:</span>
                <span class="fsns-footer-value">{{report_date}}</span>
            </div>
            <div class="fsns-footer-item">
                <span class="fsns-footer-label">Time:</span>
                <span class="fsns-footer-value">{{time}}</span>
            </div>
            <div class="fsns-footer-item">
                <span class="fsns-footer-label">Page:</span>
                <span class="fsns-footer-value">{{page_number}}</span>
            </div>
        </footer>
    </div>

</body>

</html>