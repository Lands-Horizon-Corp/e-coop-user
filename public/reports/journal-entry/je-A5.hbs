<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Journal Entry - A5</title>
<style>
    @page {
        margin: 0;
    }

    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    html, body {
        width: 5.83in;
        height: 8.27in;
        margin: 0;
        padding: 0;
    }

    body {
        background-color: #f9fafb;
        padding: 0.15in;
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .je-container {
        /* Fixed font sizes for A5 (5.83x8.27 in) */
        --fs-xs: 7px;
        --fs-sm: 8px;
        --fs-base: 9px;
        --fs-lg: 10px;
        --fs-xl: 11px;
        --fs-2xl: 14px;

        width: 5.83in;
        height: 8.27in;
        padding: 0.3in;
        background: white;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif;
        color: #1f2937;
        font-size: var(--fs-base);
        line-height: 1.3;
        display: flex;
        flex-direction: column;
        box-sizing: border-box;
    }

    /* Header */
    .je-header {
        text-align: center;
        margin-bottom: 0.3rem;
        padding-bottom: 0.2rem;
        border-bottom: 1px solid #e5e7eb;
    }

    .je-header h1 {
        font-size: var(--fs-lg);
        font-weight: 800;
        color: #111827;
        margin-bottom: 0.08rem;
        letter-spacing: -0.025em;
    }

    .je-header .je-address {
        font-size: var(--fs-sm);
        color: #6b7280;
        margin: 0.08rem 0;
    }

    .je-header .je-tin {
        font-size: var(--fs-xs);
        color: #9ca3af;
    }

    .je-header h2 {
        font-size: 12px;
        font-weight: 800;
        text-transform: uppercase;
        margin-top: 0.3rem;
        color: #111827;
        letter-spacing: -0.025em;
    }

    /* Info Grid */
    .je-info-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 0.2rem;
        margin-bottom: 0.4rem;
        font-size: var(--fs-sm);
    }

    .je-info-item {
        padding: 0.15rem;
    }

    .je-info-label {
        font-size: var(--fs-sm);
        font-weight: 600;
        color: #374151;
        display: inline-block;
    }

    .je-info-value {
        font-size: var(--fs-sm);
        color: #1f2937;
        font-weight: 600;
        padding-left: 0.15rem;
    }

    .je-info-subtext {
        font-size: var(--fs-xs);
        color: #6b7280;
        margin-top: 0.08rem;
    }

    /* Table */
    .je-table-wrapper {
        width: 100%;
        overflow-x: auto;
        margin-bottom: 0.3rem;
        border-radius: 0.15rem;
        box-shadow: 0 1px 2px rgba(0, 0, 0, 0.1);
    }

    .je-table-wrapper table {
        width: 100%;
        border-collapse: collapse;
        background: white;
        font-size: var(--fs-sm);
    }

    .je-table-wrapper thead {
        background: #f9fafb;
        position: sticky;
        top: 0;
    }

    .je-table-wrapper th {
        text-transform: uppercase;
        font-size: var(--fs-xs);
        font-weight: 700;
        color: #374151;
        padding: 0.3rem 0.2rem;
        text-align: left;
        border-bottom: 1px solid #e5e7eb;
    }

    .je-table-wrapper td {
        font-size: inherit;
        padding: 0.25rem 0.2rem;
        border-bottom: 1px solid #f3f4f6;
    }

    .je-text-right {
        text-align: right;
        font-family: 'SF Mono', 'Monaco', 'Courier New', monospace;
        font-variant-numeric: tabular-nums;
    }

    .je-highlight {
        background: #dbeafe;
        font-weight: 700;
        color: #1e40af;
        border-top: 1px solid #3b82f6;
        border-bottom: 1px solid #3b82f6;
    }

    .je-totals {
        background: #f3f4f6;
        font-weight: 700;
        color: #111827;
        font-size: var(--fs-base);
        border-top: 1px solid #9ca3af;
    }

    /* Status display */
    .je-status-words {
        margin: 0.3rem 0;
        padding: 0.3rem 0.2rem;
        background: linear-gradient(to right, #fef3c7, #fde68a);
        border: 1px solid #fbbf24;
        border-radius: 0.15rem;
        color: #78350f;
        font-size: var(--fs-base);
        font-weight: 600;
        text-align: center;
        letter-spacing: 0.025em;
    }

    /* Signatures */
    .je-signature-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 0.4rem;
        margin-top: 0.5rem;
        font-size: var(--fs-sm);
    }

    .je-signature-item {
        text-align: center;
    }

    .je-signature-label {
        font-size: var(--fs-xs);
        color: #6b7280;
        margin-bottom: 0.2rem;
    }

    .je-signature-line {
        border-bottom: 1px solid #9ca3af;
        padding: 0.2rem 0;
        min-height: 1rem;
        font-weight: 500;
        font-size: var(--fs-sm);
        color: #1f2937;
        margin-top: 0.2rem;
    }

    /* Footer */
    .je-footer {
        margin-top: auto;
        padding-top: 0.3rem;
        border-top: 1px solid #e5e7eb;
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 0.2rem;
        font-size: var(--fs-xs);
        color: #6b7280;
    }

    .je-footer-item {
        display: flex;
        flex-direction: column;
        text-align: center;
    }

    .je-footer-label {
        font-weight: 600;
        color: #374151;
        margin-bottom: 0.08rem;
    }

    .je-footer-value {
        color: #1f2937;
        font-family: 'SF Mono', 'Monaco', 'Courier New', monospace;
    }

    /* Print optimization */
    @media print {
        html, body {
            width: 5.83in;
            height: 8.27in;
            margin: 0;
            padding: 0;
        }

        body {
            background-color: white;
            padding: 0;
        }

        .je-container {
            padding: 0.3in;
            box-shadow: none;
        }

        .je-table-wrapper {
            box-shadow: none;
        }

        * {
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
        }
    }
</style>
</head>
<body>
    <div class="je-container">
        <header class="je-header">
            <h1>{{header_title}}</h1>
            <p class="je-address">{{header_address}}</p>
            <p class="je-tin">TIN: {{tax_number}}</p>
            <h2>{{report_title}}</h2>
        </header>

        <div class="je-info-grid">
            <div class="je-info-item">
                <div>
                    <span class="je-info-label">Name:</span>
                    <span class="je-info-value">{{name}}</span>
                </div>
                <p class="je-info-subtext">particulars: {{particulars}}</p>
            </div>
            <div class="je-info-item">
                <p><span class="je-info-label">Voucher No:</span> <span class="je-info-value">{{voucher_no}}</span></p>
                <p><span class="je-info-label">Entry Date:</span> <span class="je-info-value">{{entry_date}}</span></p>
                <p><span class="je-info-label">Print Date:</span> <span class="je-info-value">{{print_date}}</span></p>
            </div>
        </div>

        <div class="je-table-wrapper">
            <table>
                <thead>
                    <tr>
                        <th>Account Title</th>
                        <th class="je-text-right">Debit (₱)</th>
                        <th class="je-text-right">Credit (₱)</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each account_entries}}
                    <tr class="{{#if is_highlighted}}je-highlight{{/if}}">
                        <td>{{account_title}}</td>
                        <td class="je-text-right">{{debit}}</td>
                        <td class="je-text-right">{{credit}}</td>
                    </tr>
                    {{/each}}
                    <tr class="je-totals">
                        <td>TOTAL</td>
                        <td class="je-text-right">{{total_debit}}</td>
                        <td class="je-text-right">{{total_credit}}</td>
                    </tr>
                </tbody>
            </table>
        </div>

        <div class="je-status-words">
            *** {{status}} ({{count}})***
        </div>

        <div class="je-signature-grid">
            <div class="je-signature-item">
                <div class="je-signature-line">{{prepared_by}}</div>
                <p class="je-signature-label">Prepared by:</p>
            </div>
            <div class="je-signature-item">
                <div class="je-signature-line">{{approved_by}}</div>
                <p class="je-signature-label">Approved by:</p>
            </div>
        </div>

        <footer class="je-footer">
            <div class="je-footer-item">
                <span class="je-footer-label">User ID</span>
                <span class="je-footer-value">{{user_id}}</span>
            </div>
            <div class="je-footer-item">
                <span class="je-footer-label">Report Date</span>
                <span class="je-footer-value">{{report_date}}</span>
            </div>
            <div class="je-footer-item">
                <span class="je-footer-label">Time</span>
                <span class="je-footer-value">{{time}}</span>
            </div>
        </footer>
    </div>
</body>
</html>
