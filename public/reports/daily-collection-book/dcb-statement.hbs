<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Daily Collection Book - Statement</title>
    <style>
        :root {
            --fs-xs: 9px;
            --fs-sm: 10px;
            --fs-base: 11px;
            --fs-lg: 14px;
            --fs-xl: 16px;
            --fs-2xl: 20px;
        }

        @page {
            margin: 0;
            size: 8.5in 11in;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            width: 8.5in;
            height: 11in;
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            font-size: var(--fs-base);
            line-height: 1.4;
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
        }

        body {
            background-color: #f9fafb;
            padding: 0.5in;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .dcb-container {
            --fs-xs: 9px;
            --fs-sm: 10px;
            --fs-base: 11px;
            --fs-lg: 14px;
            --fs-xl: 16px;
            --fs-2xl: 20px;
            --fs-xs: 9px;
            --fs-sm: 10px;
            --fs-base: 11px;
            --fs-lg: 14px;
            --fs-xl: 16px;
            --fs-2xl: 20px;
            display: flex;
            
            width: 8.5in;
            height: 11in;
            padding: 0.6in;
            background: white;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Inter', 'Helvetica Neue', Arial, sans-serif;
            color: #111827;
            font-size: var(--fs-base);
            line-height: 1.5;
            display: flex;
            flex-direction: column;
            box-sizing: border-box;
        }

        .dcb-header {
            text-align: center;
            margin-bottom: 1.25rem;
            padding-bottom: 0.75rem;
        }

        .dcb-header h1 {
            font-size: var(--fs-xl);
            font-weight: 700;
            color: #111827;
            margin-bottom: 0.25rem;
            letter-spacing: -0.025em;
            line-height: 1.2;
        }

        .dcb-report-title {
            font-size: 18px;
            font-weight: 800;
            color: #111827;
            margin-top: 0.375rem;
            letter-spacing: -0.025em;

        }

        .dcb-title {
            font-size: var(--fs-2xl);
            font-weight: bold;
            margin-bottom: 0.2rem;
        }

        .dcb-address {
            font-size: var(--fs-sm);
            color: #6b7280;
            margin-bottom: 0.2rem;
        }

        .dcb-tin {
            font-size: var(--fs-xs);
            color: #9ca3af;
            margin-bottom: 0.3rem;
        }

        .dcb-report-title {
            font-size: 18px;
            font-weight: bold;
            margin-top: 0.3rem;
        }

        .dcb-date-range {
            font-size: var(--fs-sm);
            margin-top: 0.3rem;
        }

        .dcb-date-range span {
            font-weight: bold;
            margin: 0 0.25rem;
        }

        .dcb-table-container {
            margin-bottom: 0.5rem;
            overflow-x: auto;
        }

        .dcb-table {
            width: 100%;
            border-collapse: collapse;
            font-size: var(--fs-base);
            background: white;
        }

        .dcb-table thead {
            background-color: #f9fafb;
        }

        .dcb-table th {
            text-transform: uppercase;
            font-size: var(--fs-xs);
            font-weight: bold;
            color: #374151;
            padding: 0.4rem 0.5rem;
            text-align: left;
            border-bottom: 2px solid #e5e7eb;
        }

        .dcb-table td {
            padding: 0.35rem 0.5rem;
            border-bottom: 1px solid #f3f4f6;
        }

        .dcb-text-right {
            text-align: right;
            font-family: monospace;
        }

        .dcb-highlight {
            background-color: #dbeafe;
            font-weight: bold;
            color: #1e40af;
        }

        .dcb-totals {
            background-color: #f3f4f6;
            font-weight: bold;
            font-size: var(--fs-base);
            border-top: 2px solid #9ca3af;
        }

        .dcb-signature-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 0.5rem;
            margin-top: 0.75rem;
        }

        .dcb-signature-box {
            text-align: center;
        }

        .dcb-signature-label {
            font-size: var(--fs-xs);
            color: #6b7280;
            margin-bottom: 0.3rem;
        }

        .dcb-signature-line {
            border-bottom: 1px solid #9ca3af;
            padding: 0.3rem 0;
            min-height: 1.5rem;
            font-weight: 500;
            margin-top: 0.3rem;
        }

        .dcb-footer {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 0.3rem;
            margin-top: auto;
            padding-top: 0.5rem;
            border-top: 1px solid #e5e7eb;
            font-size: var(--fs-xs);
            color: #6b7280;
        }

        .dcb-footer-item {
            display: flex;
            flex-direction: column;
            text-align: center;
        }

        .dcb-footer-label {
            font-weight: bold;
            color: #374151;
            margin-bottom: 0.15rem;
        }

        .dcb-footer-value {
            color: #1f2937;
            font-family: monospace;
        }

        @media print {

            html,
            body {
                width: 8.5in;
                height: 11in;
            }
        }
    </style>
</head>

<body>
    <div class="dcb-container">
        <header class="dcb-header">
            <h1>{{header_title}}</h1>
            <div class="dcb-address">{{header_address}}</div>
            <div class="dcb-tin">TIN: {{tax_number}}</div>
            <div class="dcb-report-title">{{report_title}}</div>
            <div class="dcb-date-range">
                <span>FROM:</span> {{from_date}} <span>-</span> <span>TO:</span> {{to_date}}
            </div>
        </header>

        <div class="dcb-table-container">
            <table class="dcb-table">
                <thead>
                    <tr>
                        <th>Account Title</th>
                        <th class="dcb-text-right">Debit (₱)</th>
                        <th class="dcb-text-right">Credit (₱)</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each account_entries}}
                    <tr class="{{#if is_highlighted}}dcb-highlight{{/if}}">
                        <td>{{account_title}}</td>
                        <td class="dcb-text-right">{{debit}}</td>
                        <td class="dcb-text-right">{{credit}}</td>
                    </tr>
                    {{/each}}
                    <tr class="dcb-totals">
                        <td>TOTAL</td>
                        <td class="dcb-text-right">{{total_debit}}</td>
                        <td class="dcb-text-right">{{total_credit}}</td>
                    </tr>
                </tbody>
            </table>
        </div>

        <div class="dcb-signature-grid">
            <div class="dcb-signature-box">
                <div class="dcb-signature-line">{{prepared_by}}</div>
                <div class="dcb-signature-label">Prepared by:</div>
            </div>
            <div class="dcb-signature-box">
                <div class="dcb-signature-line">{{approved_by}}</div>
                <div class="dcb-signature-label">Approved by:</div>
            </div>
            <div class="dcb-signature-box">
                <div class="dcb-signature-line">{{check_by}}</div>
                <div class="dcb-signature-label">Checked by:</div>
            </div>
        </div>

        <footer class="dcb-footer">
            <div class="dcb-footer-item">
                <span class="dcb-footer-label">User ID</span>
                <span class="dcb-footer-value">{{user_id}}</span>
            </div>
            <div class="dcb-footer-item">
                <span class="dcb-footer-label">Report Date</span>
                <span class="dcb-footer-value">{{report_date}}</span>
            </div>
            <div class="dcb-footer-item">
                <span class="dcb-footer-label">Time</span>
                <span class="dcb-footer-value">{{time}}</span>
            </div>
        </footer>
    </div>
</body>

</html>