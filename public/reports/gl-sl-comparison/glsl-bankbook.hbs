<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GL vs SL Comparison Report - Bankbook</title>
<style>
    @page {
        margin: 0;
    }

    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    html, body {
        width: 125mm;
        height: 176mm;
        margin: 0;
        padding: 0;
    }

    body {
        background-color: #f9fafb;
        padding: 8mm;
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .glsl-container {
        /* Smallest font sizes for bankbook */
        --fs-xs: 6px;
        --fs-sm: 7px;
        --fs-base: 8px;
        --fs-lg: 10px;
        --fs-xl: 11px;
        --fs-2xl: 13px;
        --primary: #008000;

        width: 125mm;
        height: 176mm;
        padding: 8mm;
        background: white;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Inter', 'Helvetica Neue', Arial, sans-serif;
        color: #111827;
        font-size: var(--fs-base);
        line-height: 1.3;
        display: flex;
        flex-direction: column;
        box-sizing: border-box;
    }

    /* Header */
    .glsl-header {
        text-align: center;
        margin-bottom: 0.5rem;
        padding-bottom: 0.3rem;
    }

    .glsl-header-title {
        font-size: var(--fs-xl);
        font-weight: 700;
        color: #111827;
        margin-bottom: 0.15rem;
        letter-spacing: -0.02em;
        line-height: 1.1;
    }

    .glsl-header-address {
        font-size: var(--fs-xs);
        color: #6b7280;
        margin: 0.1rem 0;
        line-height: 1.2;
    }

    .glsl-header-tin {
        font-size: var(--fs-xs);
        color: #9ca3af;
        margin-top: 0.05rem;
    }

    .glsl-report-title {
        font-size: var(--fs-lg);
        font-weight: 700;
        text-transform: uppercase;
        margin-top: 0.25rem;
        letter-spacing: 0.02em;
    }

    /* Period Info */
    .glsl-period-info {
        text-align: center;
        font-size: var(--fs-xs);
        color: #6b7280;
        margin-bottom: 0.4rem;
        padding: 0.2rem;
    }

    /* Table */
    .glsl-table-header {
        display: grid;
        grid-template-columns: 2fr 0.9fr 0.9fr 35px;
        gap: 0.2rem;
        padding: 0.25rem 0.3rem;
        font-size: var(--fs-xs);
        font-weight: 700;
        background: linear-gradient(to bottom, #f9fafb, #f3f4f6);
        color: #374151;
        text-transform: uppercase;
        letter-spacing: 0.015em;
        border-bottom: 1px solid #e5e7eb;
    }

    .glsl-table-header > div {
        display: flex;
        align-items: center;
    }

    .glsl-table-header > div:not(:first-child) {
        justify-content: flex-end;
    }

    .glsl-entry-row {
        display: grid;
        grid-template-columns: 2fr 0.9fr 0.9fr 35px;
        gap: 0.2rem;
        padding: 0.25rem 0.3rem;
        font-size: var(--fs-sm);
        border-bottom: 0.5px solid #f3f4f6;
    }

    .glsl-entry-row:hover {
        background: #f9fafb;
    }

    .glsl-account-title {
        color: #1f2937;
        font-size: var(--fs-xs);
    }

    .glsl-amount {
        text-align: right;
        font-family: 'SF Mono', 'Monaco', 'Courier New', monospace;
        font-variant-numeric: tabular-nums;
        color: #1f2937;
        font-size: var(--fs-xs);
    }

    .glsl-variance {
        text-align: right;
        font-family: 'SF Mono', 'Monaco', 'Courier New', monospace;
        font-variant-numeric: tabular-nums;
        font-weight: 600;
        font-size: var(--fs-xs);
    }

    .glsl-variance.positive {
        color: #dc2626;
    }

    .glsl-variance.negative {
        color: #059669;
    }

    .glsl-variance.zero {
        color: #6b7280;
    }

    /* Summary Note */
    .glsl-summary {
        margin-top: 0.5rem;
        padding: 0.3rem;
        background: #fffbeb;
        border-left: 1.5px solid #f59e0b;
        font-size: var(--fs-xs);
        color: #92400e;
        line-height: 1.2;
    }

    .glsl-summary-title {
        font-weight: 700;
        margin-bottom: 0.15rem;
    }

    /* Signatures */
    .glsl-signature-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 0.3rem;
        margin-top: 0.6rem;
        padding: 0.3rem;
        border-radius: 0.2rem;
    }

    .glsl-signature-block {
        text-align: center;
    }

    .glsl-signature-label {
        font-size: var(--fs-xs);
        color: #6b7280;
        margin-bottom: 0.2rem;
        text-transform: uppercase;
        letter-spacing: 0.015em;
        font-weight: 600;
    }

    .glsl-signature-line {
        border-bottom: 0.5px solid #424e6167;
        padding: 0.2rem 0;
        min-height: 0.5rem;
        font-weight: 600;
        font-size: var(--fs-xs);
        margin-top: 0.2rem;
    }

    .glsl-footer {
        display: grid;
        grid-template-columns: 1fr 1fr 1fr;
        gap: 0.15rem;
        font-size: var(--fs-xs);
        color: #6b7280;
        border-top: 0.5px solid #e5e7eb;
        padding-top: 0.15rem;
        margin-top: auto;
        width: 100%;
    }

    .glsl-footer-left {
        text-align: left;
    }

    .glsl-footer-center {
        text-align: center;
    }

    .glsl-footer-right {
        text-align: right;
    }

    /* Print optimization */
    @media print {
        html, body {
            width: 125mm;
            height: 176mm;
            margin: 0;
            padding: 0;
        }

        body {
            background-color: white;
            padding: 0;
        }

        .glsl-container {
            padding: 8mm;
            box-shadow: none;
        }

        * {
            -webkit-print-color-adjust: exact;
            print-color-adjust: exact;
        }
    }
</style>
</head>
<body>
    <div class="glsl-container">
        <header class="glsl-header">
            <h1 class="glsl-header-title">{{header_title}}</h1>
            <p class="glsl-header-address">{{header_address}}</p>
            <p class="glsl-header-tin">TIN: {{tax_number}}</p>
            <h2 class="glsl-report-title">GL vs SL Report</h2>
        </header>

        <div class="glsl-period-info">
            As of: {{as_of_date}}
        </div>

        <div class="glsl-table-header">
            <div>Account</div>
            <div>GL</div>
            <div>SL</div>
            <div>Var</div>
        </div>

        {{#each account_entries}}
        <div class="glsl-entry-row">
            <div class="glsl-account-title">{{account_title}}</div>
            <div class="glsl-amount">{{gl_amount}}</div>
            <div class="glsl-amount">{{sl_amount}}</div>
            <div class="glsl-variance {{#if (equals variance 0)}}zero{{else}}{{#if variance}}{{#if (equals variance variance)}}{{#if variance}}positive{{/if}}{{/if}}{{/if}}{{/if}}">
                {{#if (equals variance 0)}}
                    -
                {{else}}
                    {{variance}}
                {{/if}}
            </div>
        </div>
        {{/each}}

        <div class="glsl-summary">
            <div class="glsl-summary-title">Note:</div>
            <div>Var = GL - SL. +: GL higher, -: SL higher.</div>
        </div>

        <div class="glsl-signature-grid">
            <div class="glsl-signature-block">
                <div class="glsl-signature-line">{{prepared_by}}</div>
                <p class="glsl-signature-label">Prepared</p>
            </div>
            <div class="glsl-signature-block">
                <div class="glsl-signature-line">{{check_by}}</div>
                <p class="glsl-signature-label">Checked</p>
            </div>
            <div class="glsl-signature-block">
                <div class="glsl-signature-line">{{approved_by}}</div>
                <p class="glsl-signature-label">Approved</p>
            </div>
        </div>

        <div class="glsl-footer">
            <div class="glsl-footer-left">{{user_id}}</div>
            <div class="glsl-footer-center">{{report_date}}</div>
            <div class="glsl-footer-right">Pg {{page_number}}</div>
        </div>
    </div>
</body>
</html>